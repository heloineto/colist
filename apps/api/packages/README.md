# Packages

Folders for self-contained utilities not tied to any business domain. They follow the structure of npm packages but are **not** actual npm packages

## Rules

- Use barrel files for exports
- Do not add separate `package.json` files to these folders
