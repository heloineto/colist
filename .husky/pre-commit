branch="$(git symbolic-ref HEAD 2>/dev/null)"

if [ "$branch" = "refs/heads/main" ]; then
    echo "Commits to main are not allowed. Work on dev or a feature branch and open a PR."
    exit 1
fi

bunx validate-branch-name || exit 1
